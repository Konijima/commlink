# Shrinking is off for now (see `isMinifyEnabled` in build.gradle.kts), so this
# file holds no rules yet. It ships because the release build type names it, and
# because turning shrinking on later is a one-line change that should have
# somewhere to put its keep rules.
