<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- The connection has to outlive the screen, and a foreground service is the only way
         Android lets an app hold a socket open when the user is not looking at it. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- A runtime permission from Android 13. Without it the app still receives messages;
         it just cannot show them. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Commlink">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- `specialUse`, because none of the defined types is what this is: the service
             exists to keep one long-lived connection to the user's own server open so that
             messages arrive when they are published. It is not syncing data on a schedule,
             and the alternative — waking on a polling alarm — is the battery cost this
             design exists to avoid. -->
        <service
            android:name=".service.SubscriberService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Maintains the subscriber's connection to its notification server, so that a published message is delivered when it is published." />
        </service>
    </application>

</manifest>
